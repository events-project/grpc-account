{
  "name": "@events-project/grpc-account",
  "version": "1.0.0",
  "description": "gRPC client for account service",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "service.proto"],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/events-project/grpc-account.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "prebuild": "mkdir -p dist && cp ../service.proto ./",
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["grpc", "account"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grpc/grpc-js": "^1.9.0",
    "@grpc/proto-loader": "^0.7.10"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.2.2"
  }
}
